## Do not make changes unless you have the appropriate permissions.

all .sh files in this directory will be run when PR's into QA or development are submitted.

Note that the GitHub runner is on Ubuntu- meaning that these shell scripts must be written in **bash**

echo statements will show in the logs section, when you expand the section with the given *name*